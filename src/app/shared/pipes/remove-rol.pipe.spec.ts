import { RemoveRolPipe } from './remove-rol.pipe';

describe('RemoveRolPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveRolPipe();
    expect(pipe).toBeTruthy();
  });
});
